# Synix LLM configuration
# Copy this file to .env and fill in your values

# Default LLM provider: "anthropic", "openai", "deepseek", or "openai-compatible"
# This is the global default — individual layers can override via llm_config
SYNIX_LLM_PROVIDER=anthropic
SYNIX_LLM_MODEL=claude-haiku-4-5-20251001

# Anthropic API key (default provider + core synthesis layer)
ANTHROPIC_API_KEY=your-anthropic-key-here

# OpenAI API key (used for embeddings and openai/openai-compatible providers)
# OPENAI_API_KEY=your-openai-key-here

# DeepSeek API key (if using deepseek as provider)
# DEEPSEEK_API_KEY=your-deepseek-key-here
